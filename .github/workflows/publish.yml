name: Publish package

on:
  push:
    tags: ["*"]

jobs:
  github:
    uses: tree-sitter/workflows/.github/workflows/release.yml@main
    with:
      attestations: true
    permissions:
      contents: write
      id-token: write
      attestations: write
