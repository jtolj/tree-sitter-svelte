name: Publish package

on:
  push:
    tags: ["*"]

jobs:
  github:
    uses: tree-sitter/workflows/.github/workflows/release.yml@main
    permissions:
      contents: write
      id-token: write
      attestations: write
